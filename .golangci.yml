version: "2"

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocyclo
    - revive

  settings:
    gocyclo:
      min-complexity: 30
    errcheck:
      exclude-functions:
        - io.Closer.Close
        - io.ReadCloser.Close
        - (*os.File).Close
        - (*database/sql.DB).Close
        - (*database/sql.Rows).Close
        - (*database/sql.Stmt).Close
        - (*database/sql.Tx).Rollback
        - os.Remove
        - os.RemoveAll
        - (*compress/gzip.Reader).Close
        - (*archive/zip.ReadCloser).Close
        - (*github.com/svtech-code/sv-memory/internal/db.Pool).Close
    govet:
      enable-all: true
      disable:
        - fieldalignment
    revive:
      rules:
        - name: exported
          disabled: true
        - name: package-comments
          disabled: true

  exclusions:
    rules:
      # Exclude cyclo complexity, errcheck, and revive checks on tests as they are more relaxed
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - revive

formatters:
  enable:
    - gofmt
    - goimports
